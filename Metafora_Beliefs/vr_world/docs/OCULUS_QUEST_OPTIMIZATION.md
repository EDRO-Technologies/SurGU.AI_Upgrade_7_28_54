# ⚡ ОПТИМИЗАЦИЯ ДЛЯ OCULUS QUEST
## Технические спецификации и производительность для standalone VR

---

## 🎯 ЦЕЛЕВОЕ УСТРОЙСТВО

### Oculus Quest 3 (рекомендуется)
- **Процессор:** Snapdragon XR2 Gen 2
- **RAM:** 8GB
- **Разрешение:** 2064 × 2208 на глаз (4128 × 2208 total)
- **Частота обновления:** 72Hz (базовый) / 90Hz / 120Hz
- **Хранилище:** 128GB / 256GB / 512GB

### Oculus Quest 2 (поддержка)
- **Процессор:** Snapdragon XR2 Gen 1
- **RAM:** 6GB
- **Разрешение:** 1832 × 1920 на глаз
- **Частота обновления:** 72Hz / 90Hz / 120Hz

---

## 📊 ЦЕЛЕВЫЕ ПОКАЗАТЕЛИ ПРОИЗВОДИТЕЛЬНОСТИ

### Обязательные требования:
- **FPS:** 72 FPS стабильно (минимум)
- **Целевой FPS:** 90 FPS
- **Батарея:** 2+ часа активного использования
- **Загрузка локации:** < 5 секунд
- **Размер приложения:** < 2GB

### Бюджет производительности на кадр (72 FPS):
- **Общее время кадра:** 13.9 ms
- **GPU время:** < 11 ms
- **CPU время:** < 8 ms
- **Headroom:** 2-3 ms (для пиков)

---

## 🎨 ГРАФИЧЕСКАЯ ОПТИМИЗАЦИЯ

### 1. ПОЛИГОНАЛЬНЫЙ БЮДЖЕТ

#### На сцену (всё видимое):
- **Quest 3:** 200,000 - 300,000 полигонов
- **Quest 2:** 150,000 - 200,000 полигонов

#### По элементам:
```
Главная площадь:
├─ Фонтан: 5,000 полигонов
├─ Статуя (x3): 8,000 полигонов каждая = 24,000
├─ Колонны (x20): 1,500 каждая = 30,000
├─ Пол/стены: 10,000
├─ AI-наставник: 5,000
├─ Декор (всё остальное): 20,000
└─ ИТОГО: ~94,000 полигонов ✅

Площадь Теней:
├─ Аркада: 40,000
├─ Обелиски (x5): 3,000 каждый = 15,000
├─ Теневые фигуры (x10): 2,000 каждая = 20,000
├─ Книга Теней: 3,000
├─ Зеркало-портал: 4,000
├─ Пол/стены: 15,000
└─ ИТОГО: ~97,000 полигонов ✅

Сады Метаморфоз (самая тяжёлая локация):
├─ Деревья (x10): 8,000 каждое = 80,000
├─ Растения/трава: 30,000
├─ Фонтаны (x3): 6,000 каждый = 18,000
├─ Статуи-метаморфозы (x3): 10,000 = 30,000
├─ Беседка: 12,000
├─ Бабочки/частицы: 5,000
├─ Земля/дорожки: 8,000
└─ ИТОГО: ~183,000 полигонов ✅
```

#### Техники оптимизации полигонов:
1. **LOD (Level of Detail) система:**
   ```csharp
   LOD 0 (близко, 0-5м): 100% полигонов
   LOD 1 (средне, 5-15м): 50% полигонов
   LOD 2 (далеко, 15-30м): 25% полигонов
   LOD 3 (очень далеко, 30м+): billboard/impostor
   ```

2. **Occlusion Culling:**
   - Не рендерить объекты за стенами/колоннами
   - Unity Occlusion Culling (запечь заранее)

3. **Frustum Culling:**
   - Автоматическое отсечение объектов вне поля зрения

---

### 2. ТЕКСТУРЫ И МАТЕРИАЛЫ

#### Бюджет памяти текстур:
- **Quest 3:** 1.5 - 2 GB
- **Quest 2:** 1 - 1.5 GB

#### Размеры текстур:
```
Приоритет 1 (близкие объекты):
- Фонтан, статуи: 2048x2048 (Diffuse + Normal)
- AI-наставник: 1024x1024
- Пол главной площади: 2048x2048 (тайлится)

Приоритет 2 (средние объекты):
- Колонны: 1024x1024
- Деревья: 1024x2048
- Обелиски: 1024x1024

Приоритет 3 (далёкие/мелкие):
- Декор: 512x512
- Мелкие камни, трава: 256x256 (атлас)

Skybox:
- 2048x2048 (кубическая карта)
```

#### Сжатие текстур:
```csharp
// Unity Texture Import Settings для Quest
TextureImporterPlatformSettings settings = new TextureImporterPlatformSettings {
    name = "Android",
    overridden = true,
    format = TextureImporterFormat.ASTC_6x6,  // Лучший баланс для Quest
    maxTextureSize = 2048,
    compressionQuality = (int)TextureCompressionQuality.Normal
};
```

#### Материалы:
- **Использовать:** URP (Universal Render Pipeline)
- **Шейдеры:** Максимально простые (Unlit, SimpleLit)
- **Избегать:** PBR со сложными расчётами, многопроходные шейдеры

```csharp
// Оптимизированный шейдер для Де Кирико эстетики
Shader "Custom/DeChiricoOptimized" {
    Properties {
        _MainTex ("Albedo", 2D) = "white" {}
        _Color ("Color", Color) = (1,1,1,1)
        _ShadowStrength ("Shadow Strength", Range(0,1)) = 0.8
    }
    SubShader {
        Tags { "RenderType"="Opaque" "RenderPipeline"="UniversalPipeline" }

        Pass {
            // Максимально простой расчёт освещения
            // Статичные тени (запечённые)
            // Без real-time отражений
        }
    }
}
```

---

### 3. ОСВЕЩЕНИЕ

#### Стратегия освещения:
- **100% запечённое освещение (Baked Lighting)**
- **НЕТ** real-time теней
- **НЕТ** real-time отражений (использовать Reflection Probes)

#### Настройка:
```csharp
// Unity Lighting Settings
Lighting Settings:
├─ Realtime Global Illumination: ВЫКЛ
├─ Baked Global Illumination: ВКЛ
├─ Lightmapper: Progressive GPU
├─ Lightmap Resolution: 10-20 texels/unit
├─ Lightmap Size: 2048x2048 (максимум)
└─ Ambient Occlusion: ВКЛ (запечённое)

// Все источники света
Light Type: Directional (солнце)
Mode: Baked
Shadows: None (тени запечены в lightmap)
```

#### Специфика для Де Кирико:
- **Застывшее время = статичное освещение** (идеально для baked!)
- Солнце в позиции 15:00 всегда → одна lightmap на локацию
- Длинные тени — часть геометрии или декали, а не real-time

---

### 4. ЧАСТИЦЫ И ЭФФЕКТЫ

#### Бюджет частиц:
- **Максимум одновременно:** 5,000 частиц
- **На один эффект:** 500-1000 частиц

#### Примеры:
```
Фонтан (вода):
- 800 частиц
- Simple Particle System (не VFX Graph)
- Billboards, а не mesh particles

Бабочки в Садах:
- 50 бабочек × 2 частицы каждая = 100 частиц
- Простая анимация (sin-wave движение)

Портал:
- 1000 частиц
- Простой шейдер (unlit + alpha blend)
```

#### Оптимизация:
```csharp
// Unity Particle System Settings
ParticleSystemRenderer settings:
├─ Render Mode: Billboard (не Mesh)
├─ Material: Unlit/Transparent
├─ Max Particles: 1000
├─ Emission: Burst (не Continuous где возможно)
└─ Collision: ВЫКЛ (очень дорого на Quest)
```

---

## 🧠 ОПТИМИЗАЦИЯ AI И СЕТИ

### 1. ОБРАБОТКА ГОЛОСА

#### STT (Speech-to-Text):
**Проблема:** Whisper API требует интернет, задержка 1-3 секунды

**Решение для Quest:**
```python
# Гибридный подход
if internet_available and latency < 2s:
    use_whisper_api()  # Лучшее качество
else:
    use_local_model()  # Локальная модель на устройстве

# Локальная модель: Whisper Tiny (39MB)
# Качество: 85-90% точности (приемлемо)
# Задержка: 0.5-1 сек
```

#### TTS (Text-to-Speech):
**Проблема:** Генерация голоса в реальном времени тяжёлая

**Решение:**
```python
# Кеширование часто используемых фраз
common_phrases = {
    "welcome": "audio/welcome.mp3",
    "what_do_you_feel": "audio/what_feel.mp3",
    # ... 100-200 заранее сгенерированных фраз
}

# Для уникальных фраз — API (требует интернет)
# Для общих — проигрывание из кеша (мгновенно)
```

---

### 2. AI BACKEND

#### Архитектура:
```
┌─────────────────────────────────────┐
│      QUEST (Клиент)                 │
│  - Рендеринг мира                   │
│  - Локальный STT (Whisper Tiny)     │
│  - Кеш TTS аудио                    │
└─────────────────┬───────────────────┘
                  │ WebSocket / HTTP
                  ▼
┌─────────────────────────────────────┐
│   BACKEND SERVER (Cloud/Local)      │
│  - GPT-4 для AI reasoning           │
│  - ElevenLabs для TTS               │
│  - Обновление beliefs.csv           │
└─────────────────────────────────────┘
```

#### Оптимизация задержек:
1. **Предзагрузка контекста:**
   - При входе в локацию → сразу загрузить возможные AI-ответы
   - Кеш последних 10 диалогов

2. **Streaming ответа:**
   ```python
   # Вместо ожидания полного ответа
   for chunk in ai_stream_response():
       send_to_tts(chunk)  # Начать озвучку сразу
   ```

3. **Оффлайн режим:**
   - Базовые фразы работают без интернета
   - Расширенный AI требует интернет
   - Предупреждение пользователя о режиме

---

## 🔋 ЭНЕРГОПОТРЕБЛЕНИЕ И БАТАРЕЯ

### Цель: 2+ часа на одном заряде

#### Техники экономии:
1. **Динамическое разрешение:**
   ```csharp
   // Если GPU перегружен, снизить разрешение
   if (GPU.FrameTime > 13ms) {
       RenderScale = Mathf.Max(0.7f, RenderScale - 0.05f);
   } else if (GPU.FrameTime < 10ms) {
       RenderScale = Mathf.Min(1.0f, RenderScale + 0.02f);
   }
   ```

2. **Foveated Rendering:**
   ```csharp
   // Центр зрения — высокое качество
   // Периферия — низкое качество
   OVRManager.tiledMultiResLevel = OVRManager.TiledMultiResLevel.LMSHigh;
   // Экономия: 15-20% GPU
   ```

3. **Adaptive Performance:**
   ```csharp
   // Unity Adaptive Performance Package
   // Автоматически снижает качество при перегреве
   ```

---

## 📦 РАЗМЕР ПРИЛОЖЕНИЯ

### Цель: < 2GB

#### Текущая оценка:
```
├─ Базовый движок Unity: 150 MB
├─ Текстуры (сжатые ASTC): 800 MB
├─ Модели 3D: 200 MB
├─ Аудио (AI голос кеш): 300 MB
├─ Локальная AI модель: 50 MB
├─ Прочее (скрипты, конфиги): 50 MB
└─ ИТОГО: ~1.55 GB ✅
```

#### Оптимизация размера:
1. **Asset Bundles:**
   - Базовая локация (Главная площадь): встроена
   - Другие локации: загружаются по требованию
   ```csharp
   // При входе в портал
   await LoadLocationAssetBundle("plaza_shadows");
   ```

2. **Сжатие аудио:**
   - Формат: Vorbis (OGG)
   - Битрейт: 96-128 kbps (голос)
   - Стерео → моно (голос AI)

3. **Текстуры:**
   - ASTC 6x6 (хороший баланс качества/размера)
   - Mipmap chain (для LOD)

---

## 🛠️ UNITY НАСТРОЙКИ

### Project Settings для Quest:

```csharp
// Build Settings
Platform: Android
Texture Compression: ASTC
Target API Level: Android 10 (API 29)

// Player Settings
Color Space: Linear
Graphics API: Vulkan (OpenGL ES 3 fallback)
Multithreaded Rendering: ON
Stereo Rendering Mode: Multiview

// XR Settings
XR Plugin: Oculus
Depth Submission: OFF (экономия производительности)
Shared Depth Buffer: OFF

// Quality Settings
Anti Aliasing: MSAA 2x (Quest 3) / OFF (Quest 2)
Anisotropic Filtering: Per Texture
Shadow Distance: 0 (все тени запечены)
Pixel Light Count: 0 (все освещение запечено)

// Physics
Fixed Timestep: 0.02 (50 FPS для физики, достаточно)
Max Allowed Timestep: 0.1

// Audio
DSP Buffer Size: Best Performance (1024)
Virtual Voice Count: 64
Real Voice Count: 32
```

---

## 🧪 ПРОФИЛИРОВКА И ТЕСТИРОВАНИЕ

### Инструменты:

1. **Unity Profiler (на устройстве):**
   ```
   Build Settings → Development Build + Autoconnect Profiler
   Подключить Quest через Wi-Fi → Window → Analysis → Profiler
   ```

2. **OVR Metrics Tool:**
   - Показывает FPS, температуру, GPU/CPU usage
   - Встроен в Quest, включить в настройках разработчика

3. **Oculus Performance HUD:**
   ```csharp
   // В игре нажать Home + Trigger + X (левый контроллер)
   // Или включить через код:
   OVRManager.cpuLevel = 4;  // Max CPU performance
   OVRManager.gpuLevel = 4;  // Max GPU performance
   ```

### Чек-лист перед релизом:

```
✅ Стабильно 72 FPS во всех локациях
✅ Нет провалов FPS > 5ms
✅ Температура < 45°C после 30 минут
✅ Батарея: < 50% за 1 час использования
✅ Загрузка локации < 5 секунд
✅ Нет tearing, jitter, judder
✅ Комфорт (не вызывает motion sickness)
✅ Текст читабелен на расстоянии 2-3 метра
✅ Голос AI слышен чётко
✅ Нет audio clipping, distortion
```

---

## 🎮 ПОЛЬЗОВАТЕЛЬСКИЙ КОМФОРТ

### Предотвращение Motion Sickness:

1. **Статичный горизонт:**
   - Главная площадь, Башня — всегда видны горизонтальные линии
   - Помогает вестибулярному аппарату

2. **Виньетирование при движении:**
   ```csharp
   // Затемнение краёв экрана при телепортации
   if (isTeleporting) {
       vignette.intensity = 0.5f;
   }
   ```

3. **Плавная телепортация:**
   - Не резкие скачки, а fade out → fade in
   - Длительность: 0.5-1 секунда

4. **Статичная точка отсчёта:**
   - AI-наставник всегда виден → anchor point для глаз
   - Фонтан на главной площади — центр координат

---

## 📋 РЕКОМЕНДУЕМЫЙ ПЛАН ОПТИМИЗАЦИИ

### Фаза 1: MVP (Quest 3, 72 FPS)
1. Главная площадь + 1 локация (Площадь Теней)
2. Baked lighting
3. LOD system
4. Базовый AI (с интернетом)

### Фаза 2: Расширение (Quest 3, 90 FPS)
1. Все 7 локаций
2. Asset Bundle система
3. Локальный STT
4. Кеш TTS

### Фаза 3: Поддержка Quest 2
1. Снижение разрешения текстур
2. Упрощение геометрии (LOD 1 как базовый)
3. Foveated rendering
4. Динамическое разрешение

---

## 🔧 ПРИМЕР СКРИПТА ОПТИМИЗАЦИИ

```csharp
using UnityEngine;
using Unity.XR.Oculus;

public class QuestOptimizer : MonoBehaviour {
    [Header("Target Performance")]
    public int targetFPS = 72;
    public float targetFrameTime = 13.9f; // ms

    [Header("Dynamic Settings")]
    public float currentRenderScale = 1.0f;
    public int currentLODBias = 0;

    private float[] frameTimeSamples = new float[60];
    private int sampleIndex = 0;

    void Update() {
        // Замер производительности
        float frameTime = Time.deltaTime * 1000f;
        frameTimeSamples[sampleIndex] = frameTime;
        sampleIndex = (sampleIndex + 1) % frameTimeSamples.Length;

        float avgFrameTime = GetAverageFrameTime();

        // Динамическая адаптация
        if (avgFrameTime > targetFrameTime + 2) {
            // Производительность падает
            ReduceQuality();
        } else if (avgFrameTime < targetFrameTime - 3) {
            // Есть запас — можно повысить качество
            IncreaseQuality();
        }

        // Мониторинг
        if (Input.GetKeyDown(KeyCode.P)) {
            LogPerformance();
        }
    }

    void ReduceQuality() {
        // Шаг 1: Снизить render scale
        if (currentRenderScale > 0.7f) {
            currentRenderScale -= 0.05f;
            UnityEngine.XR.XRSettings.renderViewportScale = currentRenderScale;
            Debug.Log($"Reduced render scale to {currentRenderScale}");
            return;
        }

        // Шаг 2: Увеличить LOD bias (более агрессивный LOD)
        if (currentLODBias < 2) {
            currentLODBias++;
            QualitySettings.lodBias = 1.0f - (currentLODBias * 0.3f);
            Debug.Log($"Increased LOD bias to {currentLODBias}");
            return;
        }

        Debug.LogWarning("Performance critical! All optimizations applied.");
    }

    void IncreaseQuality() {
        // Обратный процесс
        if (currentLODBias > 0) {
            currentLODBias--;
            QualitySettings.lodBias = 1.0f - (currentLODBias * 0.3f);
            return;
        }

        if (currentRenderScale < 1.0f) {
            currentRenderScale += 0.02f;
            UnityEngine.XR.XRSettings.renderViewportScale = currentRenderScale;
        }
    }

    float GetAverageFrameTime() {
        float sum = 0;
        foreach (float sample in frameTimeSamples) {
            sum += sample;
        }
        return sum / frameTimeSamples.Length;
    }

    void LogPerformance() {
        float avgFPS = 1000f / GetAverageFrameTime();
        Debug.Log($"=== PERFORMANCE ===");
        Debug.Log($"Average FPS: {avgFPS:F1}");
        Debug.Log($"Render Scale: {currentRenderScale}");
        Debug.Log($"LOD Bias: {currentLODBias}");
        Debug.Log($"GPU Time: {Performance.GetGPUFrameTime():F2}ms");
        Debug.Log($"CPU Time: {Performance.GetCPUFrameTime():F2}ms");
    }
}
```

---

## ✅ ФИНАЛЬНАЯ СПЕЦИФИКАЦИЯ

```yaml
ПЛАТФОРМА: Oculus Quest 3 (primary), Quest 2 (supported)
TARGET FPS: 72 (minimum), 90 (target)
РАЗРЕШЕНИЕ: Dynamic (0.7x - 1.0x native)
ПОЛИГОНЫ: 150,000 - 300,000 на сцену
ТЕКСТУРЫ: 1.5 GB бюджет, ASTC 6x6
ОСВЕЩЕНИЕ: 100% Baked
ТЕНИ: Baked в lightmaps
ЧАСТИЦЫ: < 5,000 одновременно
БАТАРЕЯ: 2+ часа
РАЗМЕР APK: < 2 GB
ИНТЕРНЕТ: Опционально (для расширенного AI)
```

---

**Следующие шаги:**
1. Создать прототип сцены в Unity
2. Запечь освещение
3. Профилировать на Quest 3
4. Итерировать оптимизации
